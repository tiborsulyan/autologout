tiborsulyan-autologout:
  admin:
    settings:
      logoutAfter: Inactivity period to log out users (in minutes)
      warnAfter: Inactivity period for the warning message to appear (in minutes)
    validation:
      logoutTimeoutNotSet: Logout timeout should be also set if warn timeout is set
      logoutTimeoutTooBig: Logout timeout cannot be greater than the session lifetime (sessionLifetime minutes)
      warnTimeoutTooBig: Warning timeout should be smaller than logout timeout

  forum:
    warning-modal:
      title: Your session is about to expire
      remaining: "Remaining time:"
      stay: Stay logged in
      leave: Logout
